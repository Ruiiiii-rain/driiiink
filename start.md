# 喝了么 · 快速开始（给完全新手）

> 不需要任何编程基础。按顺序做，5 分钟内打开网页。

## 第 1 步：拿到代码

**方式 A（推荐）：**

```
git clone https://github.com/Ruiiiii-rain/driiiink.git
cd driiiink
```

**方式 B（没有 Git）：**
网页打开仓库 → 点绿色 **Code → Download ZIP** → 解压到任意文件夹 → 在该文件夹里打开终端。

## 第 2 步：装 Node.js（只需一次）

终端里输入检查是否已装：

```
node -v
```

- 能显示版本号（≥ 22.5）→ 跳过本步
- 提示"不是内部或外部命令"→ 到 https://nodejs.org/zh-cn/download 下载 **LTS 版**，一路"下一步"安装，然后重开终端

## 第 3 步：启动服务

- **Windows**：双击项目文件夹里的 **`start.bat`**（会自动打开浏览器）
- **任何系统**：在项目目录的终端输入：

```
node server.js
```

看到下面输出即成功：

```
喝了么 已启动
本机访问: http://localhost:3000
```

## 第 4 步：打开网页

浏览器访问 **http://localhost:3000**

输入昵称 → 设置密码（至少 6 位）→ 选一个饮品头像 → 点「开始喝水」。

---

## 想自己体验"两个人互催"？

1. 再开一个**新浏览器窗口**访问同一网址；
2. 点右上角「**换身份**」→ 注册第二个账号；
3. 第一个窗口「好友」页输入第二个账号的 **6 位邀请码**；
4. 对方点「同意」→ 两个窗口互相点「催TA」→ 实时互催生效。

---

## 常见小问题

| 现象 | 解决办法 |
|---|---|
| 终端出现 `ExperimentalWarning` | 正常提示，忽略 |
| 端口被占用 | 终端用 `PORT=3001 node server.js`，网页开 http://localhost:3001 |
| 网页打不开 | 服务终端窗口别关；确认命令没报红字 |
| 想清空数据重新开始 | 关闭服务后删除 `data/` 文件夹再启动 |
| 完整功能与玩法说明 | 看 `README.md` |

> 数据存在项目内 `data/data.db`，重启服务不丢失；删掉它等于重新开始。
